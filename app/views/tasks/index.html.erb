<div class="d-flex justify-content-center">
  <% if params[:my_tasks] == 'true' || (params[:frequency].nil?) %>
    <h2>My Tasks</h2>
  <% elsif params[:calendar] == 'true' %>
    <h2>My Calendar</h2>
  <% end %>
</div>


<div>
  <div class="d-flex justify-content-center">
    <div class="request-filter-bar btn-group btn-group-toggle mb-3">
      <%= link_to "My Tasks", tasks_path(my_tasks: true), class: "btn btn-outline-primary request-filter-buttons #{'active' if params[:my_tasks] == 'true'}" %>
      <%= link_to "My Calendar", tasks_path(calendar: true), class: "btn btn-outline-primary request-filter-buttons #{'active' if params[:calendar] == 'daily'}" %>
    </div>
  </div>

  <div class="d-flex my-3">
    <div style="width: 100%;">
      <%= render 'shared/progress_bar', progress_percentage: @progress_percentage %>
    </div>
  </div>
</div>

<div class="d-flex justify-content-center" style="min-height: 100vh;">
  <div class="container d-flex flex-column align-items-center">
    <%# ------Submission Modal Created / List & List copy combined ----- %>


    <%# IF HERE FOR rending submissions if 'My tasks' == true %>
    <%= render 'submissions/list', submissions: @submissions %>
    <%# ELSIF  FOR rendering calendar if 'My Calendar' == true %>
  </div>
</div>

  <%# ----- This is the code to view individual users --------- %>
  <%# <div class="request-filter-bar btn-group btn-group-toggle mb-3">
    <% @users.each do |user| %>
  <%# <%= link_to user.username, tasks_path(user_id: user.id), class: "mt-3 btn btn-outline-primary request-filter-buttons #{'active' if @user == user}" %>
  <%# <% end %>
  <%# </div> %>
