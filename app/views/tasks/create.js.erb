<% if @task.persisted? %>
  // Remove the entire card from the DOM
  document.getElementById("task-<%= @task.title.parameterize %>").remove();
<% else %>
  alert("There was an error creating the task.");
<% end %>
